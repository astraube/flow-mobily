export const isAuthenticated = ({ isAuthenticated }) => isAuthenticated
export const isReady = ({ isReady }) => isReady
export const isLoggingIn = ({ isLoggingIn }) => isLoggingIn
export const isAdmin = ({ isAdmin }) => isAdmin
